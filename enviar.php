<?php
    $nome = $_POST['nome']; //O valor que estiver no input será guardado na variável nome.
    $telefone = $_POST['telefone']; //O valor que estiver no input será guardado na variável telefone.
    $email = $_POST['email']; //O valor que estiver no input será guardado na variável email.
    $mensagem = $_POST['mensagem']; //O valor que estiver no input será guardado na variável mensagem.

    echo 'Enviado com Sucesso!';

?>